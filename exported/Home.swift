//
//  DesignTokens.swift
//  Drawgle Auto-generated
//

import SwiftUI

struct AppTheme {
    static let backgroundPrimary = Color(hex: "#FFFFFF")
    static let backgroundSecondary = Color(hex: "#F3F4F6")
    static let surfaceCard = Color(hex: "#FFFFFF")
    static let actionPrimary = Color(hex: "#3B82F6")
    static let actionOnPrimary = Color(hex: "#FFFFFF")
    static let textHigh = Color(hex: "#111827")
    static let textMedium = Color(hex: "#4B5563")
    static let textLow = Color(hex: "#9CA3AF")
    static let borderDivider = Color(hex: "#E5E7EB")
    
    static let borderRadiusApp: CGFloat = 18
    static let borderRadiusPill: CGFloat = 9999.0
    
    static let screenPadding: CGFloat = 24
    static let sectionGap: CGFloat = 24
    static let elementGap: CGFloat = 16
}

extension Color {
    init(hex: String) {
        let hex = hex.trimmingCharacters(in: CharacterSet.alphanumerics.inverted)
        var int: UInt64 = 0
        Scanner(string: hex).scanHexInt64(&int)
        let a, r, g, b: UInt64
        switch hex.count {
        case 3: // RGB (12-bit)
            (a, r, g, b) = (255, (int >> 8) * 17, (int >> 4 & 0xF) * 17, (int & 0xF) * 17)
        case 6: // RGB (24-bit)
            (a, r, g, b) = (255, int >> 16, int >> 8 & 0xFF, int & 0xFF)
        case 8: // ARGB (32-bit)
            (a, r, g, b) = (int >> 24, int >> 16 & 0xFF, int >> 8 & 0xFF, int & 0xFF)
        default:
            (a, r, g, b) = (255, 0, 0, 0)
        }
        self.init(
            .sRGB,
            red: Double(r) / 255,
            green: Double(g) / 255,
            blue:  Double(b) / 255,
            opacity: Double(a) / 255
        )
    }
}

//
//  HomeView.swift
//  Auto-generated by Drawgle
//

import SwiftUI

struct HomeView: View {
    var body: some View {
        ScrollView {
    VStack() {
        VStack() {
            VStack() {
                Button(action: {
                    // Action here
                }) {
                    HStack() {
                        Image(systemName: "arrow.left") // Lucide: arrow-left
                            .font(.system(size: 14))
                            .foregroundColor(Color(hex: "#111827"))
                    }
                    .padding(.vertical, 12)
                    .padding(.horizontal, 16)
                    .foregroundColor(Color(hex: "#111827"))
                }
                Text("Diagnostics")
                    .font(.system(size: 14))
                    .fontWeight(.semibold)
                    .foregroundColor(Color(hex: "#1A1A1A"))
                VStack(spacing: 8) {
                    Button(action: {
                        // Action here
                    }) {
                        HStack() {
                            Image(systemName: "file.text") // Lucide: file-text
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#111827"))
                        }
                        .padding(.vertical, 12)
                        .padding(.horizontal, 16)
                        .foregroundColor(Color(hex: "#111827"))
                    }
                    Button(action: {
                        // Action here
                    }) {
                        HStack() {
                            Image(systemName: "filter") // Lucide: filter
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#111827"))
                        }
                        .padding(.vertical, 12)
                        .padding(.horizontal, 16)
                        .foregroundColor(Color(hex: "#111827"))
                    }
                }
            }
            .padding(.top, 16)
            .padding(.leading, 24)
            .padding(.trailing, 24)
            .frame(height: 64)
            VStack(spacing: 32) {
                VStack() {
                    VStack(spacing: 4) {
                        Text("Heartbeat")
                            .font(.system(size: 14))
                            .fontWeight(.semibold)
                            .foregroundColor(Color(hex: "#1A1A1A"))
                        VStack(spacing: 4) {
                            Text("82")
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#1A1A1A"))
                            Text("bpm")
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#1A1A1A"))
                        }
                    }
                    VStack() {
                        VStack() {
                            Image(systemName: "star") // Lucide: star
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#111827"))
                        }
                    }
                    .frame(height: 120)
                }
                .padding(.top, 32)
                .padding(.bottom, 32)
                .padding(.leading, 32)
                .padding(.trailing, 32)
                .background(Color(hex: "#E3F2FD"))
                .cornerRadius(32)
                .frame(maxWidth: .infinity)
                VStack() {
                    VStack(spacing: 4) {
                        Text("R-R Interval")
                            .font(.system(size: 14))
                            .fontWeight(.semibold)
                            .foregroundColor(Color(hex: "#1A1A1A"))
                        VStack(spacing: 4) {
                            Text("851")
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#1A1A1A"))
                            Text("ms")
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#1A1A1A"))
                        }
                    }
                    VStack() {
                        Image(systemName: "star") // Lucide: star
                            .font(.system(size: 14))
                            .foregroundColor(Color(hex: "#111827"))
                    }
                    .frame(width: 120, height: 40)
                }
                .padding(.top, 32)
                .padding(.bottom, 32)
                .padding(.leading, 32)
                .padding(.trailing, 32)
                .background(Color(hex: "#E8F5E9"))
                .cornerRadius(32)
                .frame(maxWidth: .infinity)
                VStack(spacing: 16) {
                    VStack() {
                        Text("Results")
                            .font(.system(size: 14))
                            .fontWeight(.semibold)
                            .foregroundColor(Color(hex: "#111827"))
                        Text("Today")
                            .font(.system(size: 14))
                            .fontWeight(.semibold)
                            .foregroundColor(Color(hex: "#6B6B6B"))
                    }
                    VStack(spacing: 32) {
                        VStack() {
                            Image(systemName: "star") // Lucide: star
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#111827"))
                            Image(systemName: "star") // Lucide: star
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#111827"))
                            VStack() {
                            }
                            .background(Color(hex: "#1A1A1A"))
                            .cornerRadius(9999)
                            .frame(width: 8, height: 8)
                        }
                        .frame(width: 80, height: 80)
                        VStack(spacing: 4) {
                            Text("Optimal Balance")
                                .font(.system(size: 14))
                                .fontWeight(.semibold)
                                .foregroundColor(Color(hex: "#1A1A1A"))
                            Text("Your heart rate variability indicates a healthy recovery state.")
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#6B6B6B"))
                        }
                    }
                    Button(action: {
                        // Action here
                    }) {
                        HStack(spacing: 8) {
                            Text("View Full Report")
                                .font(.system(size: 14))
                                .fontWeight(.semibold)
                                .foregroundColor(Color(hex: "#111827"))
                            Image(systemName: "star") // Lucide: star
                                .font(.system(size: 14))
                                .foregroundColor(Color(hex: "#111827"))
                        }
                        .padding(.vertical, 12)
                        .padding(.horizontal, 16)
                        .cornerRadius(9999)
                        .foregroundColor(Color(hex: "#111827"))
                    }
                }
                .padding(.top, 32)
                .padding(.bottom, 32)
                .padding(.leading, 32)
                .padding(.trailing, 32)
                .background(Color(hex: "#FFFFFF"))
                .cornerRadius(32)
                .frame(maxWidth: .infinity)
                VStack(spacing: 12) {
                    VStack() {
                        Text("Stress")
                            .font(.system(size: 14))
                            .fontWeight(.semibold)
                            .foregroundColor(Color(hex: "#A1A1A1"))
                        VStack(spacing: 4) {
                            Text("Low")
                                .font(.system(size: 14))
                                .fontWeight(.semibold)
                                .foregroundColor(Color(hex: "#111827"))
                        }
                    }
                    .padding(.top, 16)
                    .padding(.bottom, 16)
                    .padding(.leading, 16)
                    .padding(.trailing, 16)
                    .background(Color(hex: "#FFFFFF"))
                    .cornerRadius(32)
                    VStack() {
                        Text("Recovery")
                            .font(.system(size: 14))
                            .fontWeight(.semibold)
                            .foregroundColor(Color(hex: "#A1A1A1"))
                        VStack(spacing: 4) {
                            Text("92%")
                                .font(.system(size: 14))
                                .fontWeight(.semibold)
                                .foregroundColor(Color(hex: "#111827"))
                        }
                    }
                    .padding(.top, 16)
                    .padding(.bottom, 16)
                    .padding(.leading, 16)
                    .padding(.trailing, 16)
                    .background(Color(hex: "#FFFFFF"))
                    .cornerRadius(32)
                }
            }
            .padding(.top, 32)
            .padding(.leading, 24)
            .padding(.trailing, 24)
        }
        .background(Color(hex: "#F9F9F7"))
        .frame(maxWidth: .infinity)
    }
        }
        .background(AppTheme.backgroundPrimary)
        .edgesIgnoringSafeArea(.all)
    }
}

#Preview {
    HomeView()
}
